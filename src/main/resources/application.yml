spring:
  # ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.21.78:3306/help_me_chat?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
    username: ikroya
    password: Cug283317!
  # Web??
  web:
    cors:
      allowed-origins: "*" # ????????????????
      allowed-methods: "*"
      allowed-headers: "*"

# MyBatis-Plus??
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true # ??????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ??SQL???????
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath:mapper/**/*.xml # Mapper.xml????

# ????
server:
  port: 8080